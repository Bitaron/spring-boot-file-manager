package io.github.bitaron.filemanager.api;

import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

/**
 * Placeholder for issue #28 (Multi-module Maven scaffold + CI): proves this module compiles and
 * its test phase runs under the reactor build. Replace with real DTO specs as issue #34's REST
 * surface lands.
 */
class ScaffoldSmokeTest {

    @Test
    void moduleBuildsAndTestsRun() {
        assertThat(true).isTrue();
    }
}
