/**
 * REST request/response DTOs (see ADR 0004), split into their own module so a future v2 thin
 * HTTP client can depend on the wire shapes without pulling in {@code file-manager-service}.
 */
package io.github.bitaron.filemanager.api;
