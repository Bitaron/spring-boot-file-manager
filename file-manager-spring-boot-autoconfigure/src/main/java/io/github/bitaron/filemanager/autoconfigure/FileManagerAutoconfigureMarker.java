package io.github.bitaron.filemanager.autoconfigure;

/**
 * Scaffold-only marker proving this module is on the classpath (used by the starter module's
 * scaffold test, issue #28). Real {@code @AutoConfiguration} classes replace/supplement this as
 * Embedded Mode wiring lands (issue #17's decision).
 */
public final class FileManagerAutoconfigureMarker {

    private FileManagerAutoconfigureMarker() {
    }
}
